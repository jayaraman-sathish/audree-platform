DATABASE_URL=postgresql+psycopg2://audree:audree@localhost:5432/audree
JWT_SECRET=dev-secret-change-me-audree
# Required for the LLM orchestration layer (open-ended/executive questions).
# Without it, that layer degrades gracefully to a clear "not configured" message;
# the fixed deterministic Intent Engine works fine without this key.
ANTHROPIC_API_KEY=
